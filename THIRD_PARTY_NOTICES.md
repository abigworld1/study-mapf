# THIRD_PARTY_NOTICES.md

このリポジトリが**コードを取り込んでいる**第三者成果物の一覧。

## 現状

**取り込んでいるコードはありません。**

`.references/` にある第三者実装は、挙動確認とテストケース作成のために
clone しているだけで、コードは 1 行も転記していません。
`docs/` にある論文 PDF もサイトへ同梱していません（再配布可の 5 本を含め、現時点で未同梱）。

npm 依存パッケージのライセンスは `node_modules/*/LICENSE` を参照してください。

## コードを取り込む場合の手順

`docs/sources/repositories.yaml` の `copy_allowed` が `true` のものだけが対象です
（現在 10 件、いずれも MIT）。取り込む場合は次をすべて行ってください。

1. ライセンス条文を読む
2. 著作権表示を保持する
3. NOTICE ファイルがあれば同梱する
4. 取り込んだソースファイルの先頭に出典コメントを書く
5. **このファイルへ追記する**

追記の形式:

```markdown
### <取り込み先のパス>

- 由来: <owner/repo>（<URL>）
- コミット: <40 桁 SHA>
- ライセンス: <SPDX>
- 著作権表示: <原文のまま>
- 取り込んだ範囲: <どの関数・どのファイルか>
- 改変の有無: <あり/なし。あるなら何を変えたか>
```

## 取り込んではいけないもの

- `copy_allowed` が `true` 以外のリポジトリ（現在 14 件）
- ライセンスファイルが存在しないリポジトリ（現在 5 件）
- USC 独自ライセンスの 7 件（教育・研究・非営利限定。標準的な OSS ライセンスではない）
- `primal` / `primal2`（LICENSE.md の著作権者がプロジェクトの権利者と一致しない）

これらは読んで理解し、**原論文に基づいて独立に実装**してください。
