# SOURCE_POLICY.md — 資料の取り扱い規約

このリポジトリは MAPF / Lifelong MAPF / MAPD の教育サイトを作る。
教材の正しさは「何を出典にしたか」で決まるため、資料の扱いを先に固定する。

対象は次の 3 種類である。

| 種別                 | 置き場所                           | Git 管理                                                                   |
| -------------------- | ---------------------------------- | -------------------------------------------------------------------------- |
| 原論文 PDF           | `docs/papers/<paper-id>/paper.pdf` | 原則しない（`.gitignore` 済み、再配布可と確認できたものだけ `git add -f`） |
| Marker 変換 Markdown | `docs/papers/<paper-id>/marker.md` | する                                                                       |
| 第三者の公開実装     | `.references/<repository-id>/`     | しない（`.gitignore` 済み）                                                |

以下の 12 条は、サイト本文を書くとき・コードを書くときの両方に適用される。
Claude Code と Codex はどちらもこの規約に従う（[AGENTS.md](AGENTS.md) 参照）。

---

## 1. 原論文を最優先する

手法の定義、擬似コード、計算量、理論保証は、必ず原論文を根拠にする。
サーベイ、解説記事、ブログ、他人の実装の README、`awesome-*` リスト、
そして LLM の記憶は、いずれも一次資料ではない。

`docs/sources/papers.yaml` の `source_type: primary` が原論文である。
原論文が入手できていない手法については、
「原論文未取得のため記述は暫定」とサイト側にも明記する。

## 2. サーベイは分類と背景説明に使う

サーベイ（`source_type: survey`）は、手法どうしの位置づけ、
歴史的な流れ、用語の統一に使う。個々の手法の定義や保証の根拠にはしない。

本リポジトリで基準にするサーベイは次の 3 本。

- `mapf-benchmarks-2019` — 用語・衝突種別・コスト関数の定義。**サイト全体の用語はこれに揃える。**
- `compilation-survey-2022` — SAT / SMT / ILP 系の分類。
- `lns-reevaluation-socs-2025` — LNS 系の性能比較の測定条件。

## 3. 公開実装は挙動確認とテストケース作成に使う

`.references/` にある第三者実装の用途は次の 3 つに限る。

1. 論文の擬似コードだけでは曖昧な箇所（タイブレーク、初期化、境界条件）の確認
2. 自前実装の出力と突き合わせるためのテストケース生成
3. 実行時間の桁感の把握

用途に **含まれない** もの: コードの転記、構造の丸写し、コメントの翻訳転載。

## 4. 公開実装が論文と異なる場合は両方を記録する

実装が論文と食い違うのはよくある（性能のための近似、論文にない枝刈り、
バグ、論文の後に入った改良）。片方に寄せて書かない。

食い違いを見つけたら次を記録する。

- `docs/papers/<paper-id>/metadata.yaml` の `reading.discrepancies_with_code`
- 該当する `docs/sources/repositories.yaml` エントリの `notes`

サイト本文では「論文ではこう定義されるが、公式実装 X では〜になっている」と両方書く。

## 5. Marker Markdown だけで判断しない

Marker（PDF → Markdown 変換）は便利だが、次を高頻度で壊す。

- 数式（特に添字、総和記号、条件付き確率、上下限）
- 擬似コード（インデント、行番号、`for` / `if` の対応）
- 表（列のずれ、セルの結合）
- 図（キャプションと図の対応、図中の記号）

したがって **数式・疑似コード・図表を本文へ引くときは、必ず `paper.pdf` を開いて目視照合する。**
`marker.md` は全文検索と当たりをつけるための索引として使う。

照合が済んだ論文だけ `papers.yaml` の `status` を `verified` にする。

## 6. ページ番号、節番号、Algorithm 番号を記録する

引用の単位は「論文」ではなく「論文の特定の箇所」である。
サイト本文に手法の定義や保証を書くときは、その根拠を
`docs/papers/<paper-id>/metadata.yaml` の `reading` セクションへ先に記録する。

```yaml
reading:
  algorithm_blocks:
    - { name: "CBS high level", page: 5, label: "Algorithm 1" }
  key_theorems:
    - { statement: "optimality", page: 12, label: "Theorem 1" }
```

記録が無い記述はサイトに出さない。

## 7. 推測した理論保証を書かない

完全性・最適性・有界準最適性は、**原論文が明示的に主張しているものだけ**を書く。

`docs/sources/algorithms.yaml` の `guarantees` の値は次の意味で使う。

| 値            | 意味                                                                                             |
| ------------- | ------------------------------------------------------------------------------------------------ |
| `true`        | 原論文が明示的に主張している                                                                     |
| `false`       | 原論文が「保証しない」と明示している。または、原論文は主張しているが後続の一次資料が反証している |
| `conditional` | 条件付きで成り立つと原論文が述べている（条件を `guarantee_evidence` / `notes` に書く）           |
| `unknown`     | 原論文で確認できていない                                                                         |

`true` または `conditional` を書くときは `guarantee_evidence` に根拠（節・定理番号、
または確認したアブストラクトの原文）を必ず添える。
バリデータ（`scripts/validate-sources.mjs`）はこれを機械的に強制する。

「たぶん最適だろう」「CBS 系だから完全だろう」は書かない。`unknown` のままにする。
`unknown` が多いことは恥ではない。誤った `true` が 1 つあるほうが有害である。

### 反証による `false`

原論文の主張が、後続の**査読済み一次資料**に否定されている場合がある。
このときは原論文の主張ではなく反証のほうを採り、`false` を書く。

- `guarantee_evidence` には**原論文の主張と反証した資料の両方**を、
  どちらもページ番号つきで書く。片方だけ書いてはならない。
- 反証の根拠は必ず一次資料であること。二次資料、ブログ、実装リポジトリの
  issue コメントを根拠に `false` にしてはならない。
- 解説ページでも両論文を併記する（第 4 条）。

該当例は `push-and-swap`。原論文 IJCAI 2011 p.4 Theorem 3.1 は
`n ≤ |V| − 2` で完全と主張するが、`push-and-rotate-aamas-2013` p.1 脚注と
§3（pp.2-4）が同じ条件内の反例と agent 処理順依存を示している。

この規則が無いと、規則だけを読んだ人が「原論文は完全と言っているのだから
`false` は誤りだ」と判断して差し戻してしまう。値と規則は必ず整合させる。

## 8. 「完全」「最適」「準最適」「eventually optimal」を区別する

日本語でも英語でも、この 4 つは別の概念である。混ぜない。

| 用語                              | 意味                                                         |
| --------------------------------- | ------------------------------------------------------------ |
| 完全 (complete)                   | 解が存在すれば必ず見つける。解が無ければ無いと報告する       |
| 最適 (optimal)                    | 最適コストの解を返す                                         |
| 有界準最適 (bounded suboptimal)   | 最適コストの `w` 倍以内であることを保証する                  |
| 最終的に最適 (eventually optimal) | 時間を掛ければ最適へ収束する。有限時間での最適性は保証しない |

特に注意すべき例。

- **LaCAM\*** は eventually optimal であって optimal ではない。
  サイト上でも「最終的に最適」と書き、「最適」と省略しない。
- **PIBT** の保証は「全エージェントが目的地へ到達する」という反復設定での到達性であり、
  古典的 MAPF の意味での完全性ではない。
- **Push and Swap** の完全性は対象インスタンスのクラスに条件がある。
  `push-and-rotate-aamas-2013` が処理順依存の問題を指摘しているため、必ず併記する。
- **ECBS / EECBS** は最適ではない。有界準最適である。
- **MAPF-LNS2** は自ら「理論的保証は無い」と述べている。

## 9. 第三者コードを安易にコピーしない

`.references/` のコードをコピー＆ペーストしない。
ライセンスが不明なリポジトリからは 1 行も持ち込まない。

現時点でライセンスファイルが存在しないのは次の 5 件である。

- `push-and-rotate-cbs-pp`
- `public-cppmomapf`（README に「academic and non-commercial use」の記載のみ）
- `mapf-icbs`
- `mapf-lns-unified`
- `awesome-mapf`

これらは `copy_allowed: false` として扱う。読むのは構わない。書き写すのは駄目である。

## 10. 非寛容ライセンスのコードは独立再実装する

寛容（MIT / BSD / Apache-2.0）でないライセンスのコードは、
挙動を理解したうえで自分で書き直す。行単位の翻訳は「書き直し」ではない。

本リポジトリで特に注意すべきもの。

- **USC 系**（`pbs` / `eecbs` / `rhcr` / `mapf-lns` / `mapf-lns2` / `cbsh2` / `cbsh2-rtc`）は
  MIT ではない。南カリフォルニア大学の独自ライセンスで、教育・研究・非営利目的に限定され、
  著作権表示の保持が条件、商用利用は USC Stevens Center への個別連絡が必要。
  GitHub の自動判定は `NOASSERTION` を返すため、**API の判定を信じずに実ファイルを読むこと。**
- **`primal` / `primal2`** の `LICENSE.md` は MIT 本文だが、著作権者が
  `.NET Foundation and Contributors` になっており当該プロジェクトの権利者と一致しない。
  定型文の混入と見られるため `copy_allowed: false` のままとし、
  転記が必要になったら著者へ確認する。

MIT のリポジトリ（`pibt2` / `pypibt` / `lacam*` / `pylacam` / `continuous-cbs` /
`libmultirobotplanning` / `mapf-visualizer` / `hog2`）から転記する場合も、
著作権表示とライセンス文の保持義務は残る。

## 11. 長い原文引用をサイトへ掲載しない

論文の文章をそのまま長く貼らない。定義や定理は自分の言葉で言い換え、
出典（論文 ID・ページ・節）を添える。

目安として、1 箇所あたり 1〜2 文を超える直接引用はしない。
擬似コードも、論文の行をそのまま写すのではなく、
本サイトの記法へ書き直したうえで「`<paper-id>` の Algorithm N に対応」と注記する。

図表は貼らない。必要なら自分で描き直し、「〜を参考に作図」と書く。

## 12. PDF の再配布可否が不明な場合は外部リンクだけを掲載する

`papers.yaml` の `redistribution.status` の運用は次のとおり。

| status      | 意味                                             | サイトでの扱い                                                         |
| ----------- | ------------------------------------------------ | ---------------------------------------------------------------------- |
| `allowed`   | `evidence_url` でライセンス根拠を示せる          | PDF を同梱してよい。ライセンス条件（表示・非営利・改変禁止など）は守る |
| `link-only` | 出版社等の権利表示があり再配布不可と分かっている | 外部リンクのみ                                                         |
| `unknown`   | 判断できていない                                 | 外部リンクのみ                                                         |

**`allowed` の根拠が確認できない PDF を、将来のサイト出力へ自動コピーしてはならない。**

現時点で `allowed` は 5 本のみである。

| paper-id            | ライセンス      | 条件                                   |
| ------------------- | --------------- | -------------------------------------- |
| `lacam3-aamas-2024` | CC BY 4.0       | 著者クレジット表示                     |
| `ita-cbs-mrs-2023`  | CC BY 4.0       | 著者クレジット表示                     |
| `pibt-aij-2022`     | CC BY-NC-ND 4.0 | 著者クレジット表示 + 非営利 + 改変禁止 |
| `rmca-ral-2021`     | CC BY-NC-ND 4.0 | 著者クレジット表示 + 非営利 + 改変禁止 |
| `mdd-sat-ecai-2016` | CC BY-NC 4.0    | 著者クレジット表示 + 非営利            |

### 再配布可否は PDF 本文で確認する

判定の根拠として、**出版社のランディングページや arXiv の abs ページだけを見てはならない。**
PDF が手に入った時点で 1 ページ目の権利表示を必ず読むこと。
2026-07-26 の一斉確認で、実際に判定が動いた例が 2 件出た。

- **`mdd-sat-ecai-2016`** — IOS Press の購読制と判断して `link-only` にしていたが、
  PDF 1 ページ目に「published online with Open Access by IOS Press and distributed under
  the terms of the Creative Commons Attribution Non-Commercial License 4」があり、
  `allowed`（CC BY-NC 4.0）へ**引き上げた**。
- **`ita-ecbs-socs-2024`** — arXiv の abs ページは CC BY-SA 4.0 を宣言しているが、
  PDF 本文 1 ページ目には AAAI テンプレート由来の
  「Copyright © 2024, AAAI. All rights reserved.」が入っていた。
  **権利表示が矛盾する場合は厳しい側を採る**という方針で `link-only` へ**引き下げた**。

後者は arXiv 一般に起こりうる。会議のカメラレディをそのまま arXiv へ投稿すると、
arXiv 側で選んだ CC ライセンスと、本文に焼き込まれた学会の権利表示が食い違う。
`allowed` にするのは、**両方が矛盾しないことを確認できた場合だけ**にする。

なお **arXiv の既定ライセンス（`arxiv.org/licenses/nonexclusive-distrib/1.0/`）は
第三者への再配布を許諾していない。** 「arXiv にあるから自由に使える」は誤りである。
本リポジトリの arXiv 論文の多くはこの既定ライセンスであり、`link-only` として扱う。

---

## 補足: この規約への違反を機械的に検出する

```bash
node scripts/validate-sources.mjs          # error があれば exit 1
node scripts/validate-sources.mjs --strict # warning も exit 1
```

バリデータが強制するのは主に第 7・12 条（根拠なき保証、再配布状態の未設定）と、
ID の重複・相互参照の破れ・`status` と実ファイルの不一致である。
第 1〜6 条、第 9〜11 条は人間とエージェントの判断に委ねられる。
